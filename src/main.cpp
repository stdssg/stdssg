#include <iostream>

const char nl = '\n';

int main(int argc, char* argv[]) {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);

    std::cout << "stdssg v1.0.0" << nl;

    return 0;
}
